---
phase: 05-kb-generator
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/github_pr_kb/generator.py
  - tests/test_generator.py
autonomous: true
requirements: [KB-03, KB-04]

must_haves:
  truths:
    - "An INDEX.md file is produced at kb/INDEX.md listing all articles"
    - "Index groups articles by category with ## headings and article counts"
    - "needs_review articles appear with a [review] marker in the index"
    - "Index is fully regenerated on every run (not incrementally appended)"
    - "Re-running generate with new classified data adds new index entries without duplication"
  artifacts:
    - path: "src/github_pr_kb/generator.py"
      provides: "_generate_index method in KBGenerator"
      contains: "def _generate_index"
    - path: "tests/test_generator.py"
      provides: "Index generation tests for KB-03"
      contains: "test_index_file_created"
  key_links:
    - from: "src/github_pr_kb/generator.py (_generate_index)"
      to: "kb/.manifest.json"
      via: "reads manifest to build index entries"
      pattern: "self\\._manifest"
    - from: "src/github_pr_kb/generator.py (_generate_index)"
      to: "kb/INDEX.md"
      via: "_write_atomic"
      pattern: "INDEX\\.md"
---

<objective>
Add index file generation to KBGenerator -- produce kb/INDEX.md grouped by category with counts, summaries, and [review] markers, regenerated from scratch on every run.

Purpose: Completes KB-03 (index file) and finalizes KB-04 (incremental merge includes index rebuild after new articles added).
Output: Updated generator.py with _generate_index, updated test_generator.py with index tests.
</objective>

<execution_context>
@C:\Users\galzi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\galzi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kb-generator/05-CONTEXT.md
@.planning/phases/05-kb-generator/05-RESEARCH.md
@.planning/phases/05-kb-generator/05-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 that this plan extends -->

From src/github_pr_kb/generator.py (after Plan 01):
```python
DEFAULT_CACHE_DIR = Path(".github-pr-kb/cache")

def slugify(text: str, max_len: int = 60) -> str: ...
def _write_atomic(path: Path, data: str) -> None: ...
def _yaml_str(value: str) -> str: ...

class GenerateResult(BaseModel):
    model_config = ConfigDict(extra="ignore")
    written: int
    skipped: int
    failed: list[dict[str, str]]

class KBGenerator:
    def __init__(self, cache_dir: Path = DEFAULT_CACHE_DIR, kb_dir: Path | None = None) -> None: ...
    def _load_manifest(self) -> dict[str, str]: ...
    def _save_manifest(self) -> None: ...
    def _find_classified_files(self) -> list[Path]: ...
    def _build_article(self, pr: PRRecord, comment: CommentRecord, classified: ClassifiedComment) -> str: ...
    def _resolve_slug(self, summary: str) -> str: ...
    def generate_all(self) -> GenerateResult: ...
```

Manifest format (kb/.manifest.json):
```json
{
  "101": "gotcha/avoid-circular-imports-in-middleware.md",
  "202": "code_pattern/use-retry-for-external-apis.md"
}
```

Article frontmatter fields (between triple-dash delimiters):
  pr_url, pr_title (quoted), comment_url, author (quoted), date (ISO),
  category, confidence (float), needs_review (bool), comment_id (int)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Index generation tests + implementation</name>
  <files>src/github_pr_kb/generator.py, tests/test_generator.py</files>
  <read_first>
    - src/github_pr_kb/generator.py (current KBGenerator class -- to add _generate_index and update generate_all)
    - tests/test_generator.py (existing fixtures and tests -- add index tests)
    - .planning/phases/05-kb-generator/05-CONTEXT.md (D-11 through D-14 index decisions)
    - .planning/phases/05-kb-generator/05-RESEARCH.md (Pattern 5: Index Regeneration, Pitfall 5)
  </read_first>
  <behavior>
    - test_index_file_created: after generate_all(), kb_dir/INDEX.md exists (D-11)
    - test_index_grouped_by_category: INDEX.md contains "## Gotcha (1)" heading with article link below it (D-12)
    - test_index_review_marker: article with needs_review=True has "[review]" marker in index entry (D-13)
    - test_index_regenerated_on_rerun: calling generate_all() twice produces identical INDEX.md (D-14)
    - test_index_multiple_categories: with articles in 2 categories, INDEX.md has 2 ## headings with correct counts
    - test_index_entry_has_summary_and_link: each index entry is a markdown link with one-line summary (D-12)
  </behavior>
  <action>
    1. Add index tests to test_generator.py:
       - `test_index_file_created`: run generate_all() with the existing fixture, assert (kb_dir / "INDEX.md").exists()
       - `test_index_grouped_by_category`: read INDEX.md content, assert it contains "## Gotcha (1)" (category name title-cased with count in parens)
       - `test_index_review_marker`: create a fixture with a low-confidence classified comment (confidence=0.6, needs_review=True), run generate_all(), assert INDEX.md contains "[review]" on that entry's line
       - `test_index_regenerated_on_rerun`: run generate_all() twice, read INDEX.md both times, assert content is identical
       - `test_index_multiple_categories`: create fixture with 2 classified files in different categories (gotcha + code_pattern), assert INDEX.md has both "## Gotcha (1)" and "## Code Pattern (1)"
       - `test_index_entry_has_summary_and_link`: assert INDEX.md contains a line matching pattern `- [summary text](category/slug.md)` format

    2. Add `_generate_index(self) -> None` method to KBGenerator:
       - Scans all .md files in self._kb_dir subdirectories (excluding INDEX.md)
       - For each file, parses YAML frontmatter by reading lines between first `---` and second `---`:
         - Extract `category`, `confidence`, `needs_review`, `comment_id` fields
         - Extract the first `# ` heading as the summary text
       - If frontmatter parsing fails for a file, log warning and skip (Pitfall 5, consistent with D-18)
       - Groups entries by category
       - Builds INDEX.md content:
         - Title: `# Knowledge Base Index`
         - Blank line
         - For each category (sorted alphabetically):
           - `## {Category Name Title-Cased} ({count})` -- e.g., "## Architecture Decision (3)"
           - Blank line
           - For each article (sorted by filename within category):
             - `- [{summary}]({relative_path})` -- where relative_path is like `gotcha/avoid-circular-imports.md`
             - If needs_review is "true": append ` [review]` after the link
           - Blank line
       - Writes INDEX.md to self._kb_dir / "INDEX.md" using _write_atomic

    3. Update `generate_all()` to call `self._generate_index()` AFTER saving manifest but BEFORE returning GenerateResult. This ensures INDEX.md is always regenerated (D-14) even when no new articles were written.

    4. Category name display: convert underscore-separated category values to title case for headings. E.g., "architecture_decision" -> "Architecture Decision", "code_pattern" -> "Code Pattern", "gotcha" -> "Gotcha", "domain_knowledge" -> "Domain Knowledge", "other" -> "Other". Implementation: `category.replace("_", " ").title()`.

    Run tests: `.venv/Scripts/python.exe -m pytest tests/test_generator.py -x -v`
    Then full suite: `.venv/Scripts/python.exe -m pytest tests/ -x`
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_generator.py -x -v && .venv/Scripts/python.exe -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - generator.py contains `def _generate_index(self) -> None:`
    - generator.py `generate_all` method contains call to `self._generate_index()`
    - generator.py contains `INDEX.md` string reference
    - generator.py contains `.replace("_", " ").title()` for category display names
    - test_generator.py contains `def test_index_file_created`
    - test_generator.py contains `def test_index_grouped_by_category`
    - test_generator.py contains `def test_index_review_marker`
    - test_generator.py contains `def test_index_regenerated_on_rerun`
    - test_generator.py contains `def test_index_multiple_categories`
    - `.venv/Scripts/python.exe -m pytest tests/test_generator.py -x` exits 0
    - `.venv/Scripts/python.exe -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>INDEX.md generated at kb/INDEX.md with category groupings, counts, summaries, review markers. Regenerated from scratch on every run. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_generator.py -x -v` -- all generator tests green (including index tests)
- `.venv/Scripts/python.exe -m pytest tests/ -x` -- full suite green
- INDEX.md contains category headings with counts
- INDEX.md contains [review] marker for low-confidence articles
- Running generate_all() twice produces identical INDEX.md
</verification>

<success_criteria>
- INDEX.md produced at kb/INDEX.md on every generate_all() call (KB-03)
- Index groups by category with ## headings and article counts (D-12)
- needs_review articles marked with [review] in index (D-13)
- Index fully regenerated each run, not incrementally appended (D-14)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-kb-generator/05-02-SUMMARY.md`
</output>
