---
phase: 06-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/github_pr_kb/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04

must_haves:
  truths:
    - "User can run github-pr-kb extract --repo owner/name and see a green summary line"
    - "User can run github-pr-kb classify and see a green summary line"
    - "User can run github-pr-kb generate and see a green summary line"
    - "User can run github-pr-kb run --repo owner/name and all three steps execute in sequence"
    - "All four commands respond to --help with description and all options listed"
    - "Missing GITHUB_TOKEN prints a human-readable error, not a traceback"
    - "Bad --since format prints a usage error with exit code 2"
    - "run command fails fast if a mid-pipeline step errors"
  artifacts:
    - path: "src/github_pr_kb/cli.py"
      provides: "Click group with extract, classify, generate, run commands"
      min_lines: 100
    - path: "tests/test_cli.py"
      provides: "CliRunner tests for all commands"
      min_lines: 80
  key_links:
    - from: "src/github_pr_kb/cli.py"
      to: "src/github_pr_kb/extractor.py"
      via: "lazy import of GitHubExtractor inside extract command body"
      pattern: "from github_pr_kb\\.extractor import"
    - from: "src/github_pr_kb/cli.py"
      to: "src/github_pr_kb/classifier.py"
      via: "lazy import of PRClassifier inside classify command body"
      pattern: "from github_pr_kb\\.classifier import"
    - from: "src/github_pr_kb/cli.py"
      to: "src/github_pr_kb/generator.py"
      via: "lazy import of KBGenerator inside generate command body"
      pattern: "from github_pr_kb\\.generator import"
---

<objective>
Replace the `cli = None` stub with a full Click CLI exposing four commands: `extract`, `classify`, `generate`, and `run`. Wire each command to the existing module classes with lazy imports, colored summaries, verbose logging, and actionable error messages.

Purpose: This is the user-facing surface for the entire tool. Without it, there is no way to run the pipeline.
Output: Working `cli.py` with 4 commands + `test_cli.py` with comprehensive CliRunner tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-integration/06-CONTEXT.md
@.planning/phases/06-cli-integration/06-RESEARCH.md
@.planning/phases/06-cli-integration/06-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/github_pr_kb/extractor.py:
```python
class RateLimitExhaustedError(Exception): ...

class GitHubExtractor:
    def __init__(self, repo_name: str, cache_dir: Path = DEFAULT_CACHE_DIR) -> None: ...
    def extract(self, state: str = "all", since: datetime | None = None, until: datetime | None = None) -> list[Path]: ...
    # NOTE: __init__ accesses settings.github_token at construction time
    # NOTE: extract raises RateLimitExhaustedError on retry exhaustion
```

From src/github_pr_kb/classifier.py:
```python
class PRClassifier:
    def __init__(self, cache_dir: Path = DEFAULT_CACHE_DIR, model: str = DEFAULT_MODEL, api_key: str | None = None) -> None: ...
    def classify_all(self) -> list[Path]: ...
    def print_summary(self) -> None: ...
    # Instance attrs after classify_all(): _classified_count, _cache_hit_count, _failed_count, _review_count
    # NOTE: __init__ raises ValueError if api_key is None and settings.anthropic_api_key is also None
    # NOTE: classify_all() calls self.print_summary() internally — CLI should suppress or work around this
```

From src/github_pr_kb/generator.py:
```python
class GenerateResult(BaseModel):
    written: int
    skipped: int
    failed: list[dict[str, str]]

class KBGenerator:
    def __init__(self, cache_dir: Path = DEFAULT_CACHE_DIR, kb_dir: Path | None = None) -> None: ...
    def generate_all(self) -> GenerateResult: ...
    # NOTE: __init__ lazy-imports settings for kb_output_dir if kb_dir is None
```

From src/github_pr_kb/config.py:
```python
class Settings(BaseSettings):
    github_token: str
    anthropic_api_key: str | None = None
    kb_output_dir: str = "kb"

settings = Settings()  # Module-level — raises ValidationError on import if GITHUB_TOKEN missing
```

From pyproject.toml:
```toml
[project.scripts]
github-pr-kb = "github_pr_kb.cli:cli"
# The Click group object MUST be named `cli`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CliRunner test suite stubs (Wave 0 — tests before implementation)</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - .planning/phases/06-cli-integration/06-RESEARCH.md (CliRunner patterns, test skeletons)
    - .planning/phases/06-cli-integration/06-UI-SPEC.md (exact copy strings to assert)
    - tests/conftest.py (existing test fixtures and env var setup patterns)
    - src/github_pr_kb/cli.py (current stub — confirms cli = None, tests will fail initially)
  </read_first>
  <behavior>
    - test_extract_help: exit 0, output contains --repo, --state, --since, --until, --verbose
    - test_classify_help: exit 0, output contains --verbose, "Classify cached PR comments"
    - test_generate_help: exit 0, output contains --verbose, "Generate markdown"
    - test_run_help: exit 0, output contains --repo, --verbose, "pipeline"
    - test_extract_missing_repo: exit 2 (missing required option)
    - test_extract_bad_date: exit 2, output contains "ISO date"
    - test_extract_runs: mock GitHubExtractor, exit 0, output contains "Extracted"
    - test_classify_runs: mock PRClassifier, exit 0, output contains "Classified"
    - test_generate_runs: mock KBGenerator, exit 0, output contains "Generated"
    - test_run_pipelines: mock all three, exit 0, output contains all three summaries
    - test_run_fails_fast: mock extract to raise, exit 1, classify/generate not called
    - test_extract_missing_token: no GITHUB_TOKEN, exit 1, output contains config error hint
  </behavior>
  <action>
    Create `tests/test_cli.py` with CliRunner-based tests. These tests define the behavioral contract for cli.py and MUST be created before implementation begins (Wave 0 / TDD RED phase). All tests will initially fail because cli.py is still a stub (`cli = None`).

    Use `click.testing.CliRunner(mix_stderr=False)` throughout.

    **Fixture:**
    ```python
    @pytest.fixture
    def runner():
        return CliRunner(mix_stderr=False)
    ```

    **Help text tests (CLI-04):** For each of the 4 commands, invoke `cli, ["{cmd}", "--help"]` and assert exit_code == 0. Assert the output contains the expected strings from the UI-SPEC Copywriting Contract:
    - `extract --help`: contains `--repo`, `--state`, `--since`, `--until`, `--verbose`, `Extract and cache PR comments`
    - `classify --help`: contains `--verbose`, `Classify cached PR comments`
    - `generate --help`: contains `--verbose`, `Generate markdown knowledge base`
    - `run --help`: contains `--repo`, `--verbose`, `pipeline`

    **Usage error tests (CLI-04):**
    - `test_extract_missing_repo`: invoke `["extract"]` with no --repo. Assert exit_code == 2.
    - `test_extract_bad_date`: invoke `["extract", "--repo", "o/r", "--since", "not-a-date"]`. Assert exit_code == 2 and "ISO date" in output or stderr.

    **Happy path tests (CLI-01, CLI-02, CLI-03):** Use `unittest.mock.patch` to mock the upstream classes. Since imports are lazy/inside function bodies, patch at the source module: `patch("github_pr_kb.extractor.GitHubExtractor")` for extract, `patch("github_pr_kb.classifier.PRClassifier")` for classify, `patch("github_pr_kb.generator.KBGenerator")` for generate. Also need env vars set: use `runner.invoke(cli, [...], env={"GITHUB_TOKEN": "fake", "ANTHROPIC_API_KEY": "fake"})`.

    - `test_extract_runs`: Patch `GitHubExtractor`. Set `mock_instance.extract.return_value = []`. Invoke `["extract", "--repo", "owner/repo"]` with env. Assert exit_code == 0 and "Extracted" in result.output.
    - `test_classify_runs`: Patch `PRClassifier`. Set `mock_instance.classify_all.return_value = []`, `mock_instance._classified_count = 3`, `mock_instance._cache_hit_count = 2`. Invoke `["classify"]` with env. Assert exit_code == 0 and "Classified" in result.output.
    - `test_generate_runs`: Patch `KBGenerator`. Import `GenerateResult` from `github_pr_kb.generator`. Set `mock_instance.generate_all.return_value = GenerateResult(written=5, skipped=2, failed=[])`. Invoke `["generate"]` with env. Assert exit_code == 0 and "Generated" in result.output.

    **Run pipeline test (D-01):**
    - `test_run_pipelines`: Patch all three classes. Set appropriate return values. Invoke `["run", "--repo", "owner/repo"]` with env. Assert exit_code == 0. Assert output contains "Extracted" and "Classified" and "Generated".

    **Run fail-fast test (D-10):**
    - `test_run_fails_fast`: Patch `GitHubExtractor` to raise `Exception("boom")` on `.extract()`. Invoke `["run", "--repo", "owner/repo"]` with env. Assert exit_code == 1. Assert "pipeline" or "failed" in output (case-insensitive). Verify PRClassifier and KBGenerator were NOT instantiated.

    **Config error test (CLI-04):**
    - `test_extract_missing_token`: Invoke `["extract", "--repo", "owner/repo"]` with `env={"GITHUB_TOKEN": ""}` (or unset). Assert exit_code == 1. Assert output contains "Configuration error" or "GITHUB_TOKEN" or the hint text.

    **Important notes for the executor:**
    - The `env` kwarg on `runner.invoke` REPLACES the environment (does not merge). Include all needed env vars.
    - Use `catch_exceptions=False` only if you want exceptions to propagate for debugging; for error-path tests, leave it as default (True) so ClickException is caught and reflected in exit_code.
    - If lazy imports mean the patch target differs from expected, check by reading the actual `from ... import` line in the command body and patch at that location.
    - **TDD expectation:** After this task, run `.venv/Scripts/python.exe -m pytest tests/test_cli.py -x` and confirm tests FAIL (RED phase). This is the expected outcome — cli.py is still a stub.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_cli.py --co -q</automated>
  </verify>
  <done>tests/test_cli.py exists with at least 10 test functions covering help text, usage errors, happy paths, pipeline, fail-fast, and config errors. Test collection succeeds (--co). Tests are expected to fail at this point because cli.py is still a stub — that is the correct TDD RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cli.py with all four Click commands (TDD GREEN phase)</name>
  <files>src/github_pr_kb/cli.py</files>
  <read_first>
    - tests/test_cli.py (just created in Task 1 — read to understand test expectations)
    - src/github_pr_kb/cli.py (current stub to replace)
    - src/github_pr_kb/extractor.py (GitHubExtractor constructor + extract signature)
    - src/github_pr_kb/classifier.py (PRClassifier constructor + classify_all + print_summary)
    - src/github_pr_kb/generator.py (KBGenerator constructor + generate_all + GenerateResult)
    - src/github_pr_kb/config.py (Settings class, module-level instantiation)
    - .planning/phases/06-cli-integration/06-UI-SPEC.md (exact copy for help text, summaries, errors)
  </read_first>
  <behavior>
    - `cli` is a `@click.group()` object with docstring: "Extract, classify, and generate a knowledge base from GitHub PR discussions."
    - `extract` command: accepts --repo (required), --state (Choice open/closed/all, default all, case_sensitive=False), --since (optional), --until (optional), -v/--verbose (flag). Calls GitHubExtractor(repo_name=repo).extract(state, since_dt, until_dt). Prints green summary.
    - `classify` command: accepts -v/--verbose. Calls PRClassifier().classify_all(). Prints green summary with classified_count, cache_hit_count.
    - `generate` command: accepts -v/--verbose. Calls KBGenerator().generate_all(). Prints green summary with written, skipped counts.
    - `run` command: accepts --repo (required), -v/--verbose. Calls extract then classify then generate logic sequentially. Fails fast on error, prints what succeeded.
    - Missing env var (GITHUB_TOKEN, ANTHROPIC_API_KEY) raises ClickException with friendly message.
    - Bad --since/--until raises UsageError with exit code 2.
    - RateLimitExhaustedError re-raised as ClickException.
  </behavior>
  <action>
    Replace the entire contents of `src/github_pr_kb/cli.py`. The file must contain:

    1. **Module-level imports** (safe ones only): `click`, `logging`, `sys`, `datetime` from stdlib. Do NOT import settings, GitHubExtractor, PRClassifier, KBGenerator, or any module that transitively imports `settings` at module level.

    2. **Helper function `_parse_iso_date(value: str | None, flag_name: str) -> datetime | None`**: Returns None if value is None. Otherwise calls `datetime.fromisoformat(value)`. On ValueError, raises `click.UsageError(f"--{flag_name} must be an ISO date (e.g. 2024-01-01). Got: {value!r}")`.

    3. **Helper function `_configure_logging(verbose: bool) -> None`**: If verbose, calls `logging.basicConfig(level=logging.INFO, stream=sys.stderr, format="%(message)s")`.

    4. **`@click.group()` named `cli`**: Docstring exactly: `Extract, classify, and generate a knowledge base from GitHub PR discussions.`

    5. **`@cli.command()` named `extract`** (per D-01, D-03, CLI-01):
       - Options: `--repo` (required=True, help=`GitHub repository in owner/name format. Example: pallets/click`), `--state` (default="all", type=click.Choice(["open", "closed", "all"], case_sensitive=False), show_default=True, help=`Filter PRs by state.`), `--since` (default=None, metavar="DATE", help=`Only PRs updated on or after this ISO date. Example: 2024-01-01`), `--until` (default=None, metavar="DATE", help=`Only PRs updated on or before this ISO date. Example: 2024-12-31`), `-v`/`--verbose` (is_flag=True, help=`Print per-PR detail during extraction.`)
       - Docstring: `Extract and cache PR comments from a GitHub repository.` followed by `\b` block with examples: `github-pr-kb extract --repo pallets/click` and `github-pr-kb extract --repo pallets/click --state closed --since 2024-01-01`
       - Body: call `_configure_logging(verbose)`, parse dates with `_parse_iso_date`, then inside a try/except: lazy import `from github_pr_kb.extractor import GitHubExtractor, RateLimitExhaustedError`, instantiate `GitHubExtractor(repo_name=repo)`. Catch `Exception` during construction — if it's a `pydantic.ValidationError` (check via lazy import), raise `click.ClickException("Configuration error -- missing required environment variable.\nHint: copy .env.example to .env and fill in GITHUB_TOKEN.")`. Otherwise raise `click.ClickException(f"Startup error: {exc}")`.
       - Then call `extractor.extract(state=state, since=since_dt, until=until_dt)`. Catch `RateLimitExhaustedError` -> `click.ClickException(str(exc))`. Catch generic `Exception` -> `click.ClickException(f"Extraction failed: {exc}\nHint: verify --repo is in owner/name format and your token has repo read access.")`.
       - On success, count total comments by reading each written path (lazy import PRFile from models, load JSON, sum len(pr_file.comments)). Print: `click.echo(click.style(f"Extracted {len(paths)} PRs, {total_comments} comments cached.", fg="green"))`.

    6. **`@cli.command()` named `classify`** (per D-01, D-04, CLI-02):
       - Options: `-v`/`--verbose` (is_flag=True, help=`Print per-comment detail during classification.`)
       - Docstring: `Classify cached PR comments into knowledge categories using Claude.` followed by `\b` examples block: `github-pr-kb classify` and `github-pr-kb classify --verbose`
       - Body: `_configure_logging(verbose)`. Try/except: lazy import `from github_pr_kb.classifier import PRClassifier`. Instantiate `PRClassifier()`. Catch `ValueError` (missing ANTHROPIC_API_KEY from classifier __init__) -> `click.ClickException("Configuration error -- missing required environment variable.\nHint: copy .env.example to .env and fill in ANTHROPIC_API_KEY.")`. Catch `pydantic.ValidationError` (missing GITHUB_TOKEN triggered by config import chain) -> same pattern with GITHUB_TOKEN message.
       - Call `classifier.classify_all()`. Read counts from `classifier._classified_count` and `classifier._cache_hit_count`. Total = classified + cache_hit. New = classified. Cached = cache_hit.
       - Print: `click.echo(click.style(f"Classified {total} comments ({new} new, {cached} cached).", fg="green"))`.

    7. **`@cli.command()` named `generate`** (per D-01, D-04, CLI-03):
       - Options: `-v`/`--verbose` (is_flag=True, help=`Print per-article detail during generation.`)
       - Docstring: `Generate markdown knowledge base articles from classified comments.` followed by `\b` examples block: `github-pr-kb generate` and `github-pr-kb generate --verbose`
       - Body: `_configure_logging(verbose)`. Try/except: lazy import `from github_pr_kb.generator import KBGenerator`. Instantiate `KBGenerator()`. Catch config errors same pattern.
       - Call `result = generator.generate_all()`. Total = result.written + result.skipped. Print: `click.echo(click.style(f"Generated {total} articles ({result.written} new, {result.skipped} skipped).", fg="green"))`.

    8. **`@cli.command()` named `run`** (per D-01, D-02, D-10):
       - Options: `--repo` (required=True, help=`GitHub repository in owner/name format. Example: pallets/click`), `-v`/`--verbose` (is_flag=True, help=`Print per-item detail for each pipeline step.`)
       - Docstring: `Run the full extract -> classify -> generate pipeline in one command.` (use arrow character: ->) followed by `\b` examples block: `github-pr-kb run --repo pallets/click`
       - Body: `_configure_logging(verbose)`. Call each step's logic directly (NOT via runner.invoke). Use the same lazy-import + try/except pattern as individual commands. After each successful step, print its green summary. If a step fails, raise `click.ClickException(f"Pipeline failed at {step_name} step: {exc}")`. Use helper functions `_run_extract(repo, verbose)`, `_run_classify(verbose)`, `_run_generate(verbose)` that return the summary string, to avoid duplicating the instantiation + call + error handling logic between individual commands and `run`.

    **Refactoring note:** To share logic between individual commands and `run`, extract private helper functions `_run_extract(repo: str, state: str, since: str | None, until: str | None, verbose: bool) -> str`, `_run_classify(verbose: bool) -> str`, `_run_generate(verbose: bool) -> str` that each return the summary message string. The `@cli.command()` functions become thin wrappers: parse flags, call helper, echo the returned string. The `run` command calls all three helpers sequentially.

    **TDD GREEN phase:** After implementing, run the test suite from Task 1. All tests should now pass. Fix any failures by adjusting the implementation to match test expectations.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_cli.py -x -v</automated>
  </verify>
  <done>
    - cli.py does NOT contain `from github_pr_kb.config import settings` or `from github_pr_kb.extractor import` at module level (outside function bodies)
    - cli.py contains `@click.group()` decorator on a function named `cli`
    - cli.py contains `@cli.command()` decorators for functions named `extract`, `classify`, `generate`, `run`
    - cli.py contains `def _parse_iso_date(`
    - cli.py contains `click.ClickException` for error handling (at least 3 occurrences)
    - cli.py contains `click.style(` with `fg="green"` for summary lines (at least 3 occurrences)
    - cli.py contains `click.Choice(["open", "closed", "all"]` for --state option
    - cli.py contains `--repo` option with `required=True` on extract and run commands
    - All tests in tests/test_cli.py pass (TDD GREEN)
    - Full test suite `.venv/Scripts/python.exe -m pytest tests/ -x` remains green
  </done>
</task>

</tasks>

<verification>
1. `.venv/Scripts/python.exe -m pytest tests/test_cli.py -x -v` — all CLI tests pass
2. `.venv/Scripts/python.exe -m pytest tests/ -x` — full test suite green (no regressions)
3. `.venv/Scripts/python.exe -c "from github_pr_kb.cli import cli; print(type(cli))"` — outputs `<class 'click.core.Group'>`
4. Verify no module-level imports of settings/extractor/classifier/generator in cli.py
</verification>

<success_criteria>
- All four commands (extract, classify, generate, run) are wired and respond to --help
- Help text matches UI-SPEC copywriting contract exactly
- Summary lines use green click.style for success
- Errors produce ClickException with actionable hints, not raw tracebacks
- Lazy imports prevent --help from crashing when env vars are missing
- At least 10 CliRunner tests pass covering help, happy path, errors, and pipeline
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-integration/06-01-SUMMARY.md`
</output>
