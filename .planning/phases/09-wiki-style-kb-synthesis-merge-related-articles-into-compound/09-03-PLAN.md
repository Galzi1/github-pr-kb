---
phase: 09-wiki-style-kb-synthesis-merge-related-articles-into-compound
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/github_pr_kb/generator.py
  - src/github_pr_kb/cli.py
  - tests/test_generator.py
  - tests/test_cli.py
autonomous: true
requirements: [D-05, D-12]

must_haves:
  truths:
    - "generate command synthesizes topics by default"
    - "--no-synthesize flag produces per-comment articles only (no topic synthesis)"
    - "INDEX.md links to topic pages with title from frontmatter as display text"
    - "INDEX.md uses same format: ## Category (count), bulleted links with summaries"
    - "Full pipeline: generate_all() runs per-comment pass in memory then synthesizes topics"
    - "Regenerate mode with synthesis works end-to-end"
  artifacts:
    - path: "src/github_pr_kb/cli.py"
      provides: "--no-synthesize flag on generate command"
      contains: "no_synthesize"
    - path: "src/github_pr_kb/generator.py"
      provides: "Wired generate_all with synthesis integration and updated index"
      contains: "synthesize"
    - path: "tests/test_cli.py"
      provides: "CLI test for --no-synthesize flag"
      contains: "no_synthesize"
    - path: "tests/test_generator.py"
      provides: "End-to-end generate_all with synthesis tests"
      contains: "test_generate_all_with_synthesis"
  key_links:
    - from: "cli.py:generate command"
      to: "generator.py:generate_all"
      via: "synthesize parameter"
      pattern: "generate_all.*synthesize"
    - from: "generator.py:generate_all"
      to: "generator.py:_synthesize_topics"
      via: "called after _run_generation_pass when synthesize=True"
      pattern: "_synthesize_topics"
    - from: "generator.py:_generate_index"
      to: "topic page frontmatter"
      via: "reads title from frontmatter for display text"
      pattern: "title"
---

<objective>
Wire topic synthesis into the generate pipeline, add --no-synthesize CLI flag, and update INDEX.md to link to topic pages.

Purpose: Complete the integration - connect the synthesis pass from Plan 02 into generate_all(), update the index to reflect topic pages instead of per-comment articles, and give users the --no-synthesize escape hatch.

Output: generate_all() with synthesize parameter, updated _generate_index() reading topic page titles, --no-synthesize CLI flag, and end-to-end tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-wiki-style-kb-synthesis-merge-related-articles-into-compound/09-CONTEXT.md
@.planning/phases/09-wiki-style-kb-synthesis-merge-related-articles-into-compound/09-01-SUMMARY.md
@.planning/phases/09-wiki-style-kb-synthesis-merge-related-articles-into-compound/09-02-SUMMARY.md

@src/github_pr_kb/generator.py
@src/github_pr_kb/cli.py
@tests/test_generator.py
@tests/test_cli.py

<interfaces>
<!-- From Plan 02 - synthesis methods on KBGenerator -->
def _synthesize_topics(self) -> None: ...
def _collect_in_memory_articles(self) -> list[dict[str, str]]: ...
def _build_topic_page(self, group: TopicGroup, articles: list[dict], all_slugs: list[str]) -> str | None: ...
def _strip_broken_links(self, body: str, valid_slugs: set[str]) -> str: ...

<!-- From Plan 01 - manifest is nested -->
# self._manifest = {"comments": {str_id: "cat/slug.md"}, "topics": {slug: {sources, content_hash, last_synthesized}}}

<!-- Existing generate_all signature -->
def generate_all(self, regenerate: bool = False) -> GenerateResult: ...

<!-- GenerateResult after Plan 02 -->
class GenerateResult(BaseModel):
    written: int
    skipped: int
    filtered: int = 0
    failed: list[dict[str, str]]
    topics_written: int = 0
    topics_skipped: int = 0

<!-- Existing _generate_index reads from disk -->
def _generate_index(self) -> None: ...
def _collect_index_entries(self) -> dict[str, list[IndexEntry]]: ...
def _parse_article_metadata(self, text: str) -> tuple[dict[str, str] | None, str]: ...

<!-- CLI _run_generate helper -->
def _run_generate(regenerate: bool = False) -> str: ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire synthesis into generate_all() and update _generate_index() for topic pages</name>
  <files>src/github_pr_kb/generator.py, tests/test_generator.py</files>
  <read_first>
    - src/github_pr_kb/generator.py (full file after Plan 02 - generate_all, _run_generation_pass, _generate_all_transactionally, _generate_index, _collect_index_entries, _parse_article_metadata, _synthesize_topics)
    - tests/test_generator.py (full file after Plan 02)
  </read_first>
  <behavior>
    - Test: generate_all(synthesize=True) calls _synthesize_topics after collecting articles, then generates index from topic pages
    - Test: generate_all(synthesize=False) writes per-comment articles to disk (old behavior) and generates index from them
    - Test: generate_all(regenerate=True, synthesize=True) does transactional rebuild with topic synthesis
    - Test: INDEX.md after synthesis contains links to topic pages with title from topic frontmatter as display text (per D-12)
    - Test: INDEX.md format is ## Category (count) with bulleted links (per D-12)
    - Test: GenerateResult.topics_written and topics_skipped reflect synthesis counts
    - Test: When synthesize=True, only topic page .md files exist in kb_dir (no per-comment articles)
  </behavior>
  <action>
    1. Add `synthesize: bool = True` parameter to `generate_all()`:
       ```python
       def generate_all(self, regenerate: bool = False, synthesize: bool = True) -> GenerateResult:
       ```

    2. Modify `_run_generation_pass()` to accept a `synthesize` parameter:
       - When `synthesize=True`: do NOT call `_write_article()` or `_generate_index()` at the end. Instead, after iterating classified files (to collect manifest comment tracking for skip purposes), call `_synthesize_topics()` which handles in-memory article collection, planning, synthesis, and writing topic pages. Then call `_generate_index()`.
       - When `synthesize=False`: keep current behavior exactly (write per-comment articles, generate index from them).

    3. Update `_generate_all_transactionally()` to pass through the `synthesize` parameter.

    4. Update `_generate_index()` / `_collect_index_entries()` to use `python-frontmatter` for parsing topic page frontmatter (per D-12):
       - Read `title` from frontmatter as the display text (instead of `# heading`)
       - If `title` is missing (old per-comment article), fall back to `# heading` extraction
       - INDEX.md format stays the same: `## Category (count)`, bulleted `- [title](rel_path)` with optional `[review]` marker

    5. Update `_parse_article_metadata()` to use `python-frontmatter` library instead of hand-rolled parsing (research recommendation). Keep the method signature compatible. Use `frontmatter.loads(text)` to get metadata dict and extract title/summary from `post.metadata.get("title")` or fall back to `# heading`.

    6. Wire synthesis counters into GenerateResult:
       ```python
       return GenerateResult(
           written=self._written,
           skipped=self._skipped,
           filtered=self._filtered,
           failed=self._failed,
           topics_written=self._topics_written,
           topics_skipped=self._topics_skipped,
       )
       ```
       Add `self._topics_written = 0` and `self._topics_skipped = 0` instance vars (reset in generate_all).

    7. Write end-to-end tests: mock Anthropic client for both planning and synthesis calls, set up classified files via `_write_classified_pair()`, call `generate_all(synthesize=True)`, verify:
       - Only topic pages on disk (no per-comment .md files)
       - INDEX.md links use topic title as display text
       - GenerateResult counts are correct
       - synthesize=False produces per-comment articles (backward compat)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_generator.py -x -q --no-cov -k "generate_all_with_synthesis or generate_all_no_synthesis or index_topic_pages"</automated>
  </verify>
  <acceptance_criteria>
    - grep "synthesize.*bool.*True" src/github_pr_kb/generator.py shows the parameter default in generate_all
    - grep "_topics_written" src/github_pr_kb/generator.py returns matches
    - grep "frontmatter.loads\|frontmatter.load" src/github_pr_kb/generator.py returns a match in _parse_article_metadata or _collect_index_entries
    - grep "test_generate_all_with_synthesis" tests/test_generator.py returns a match
    - All tests pass: `.venv/Scripts/python.exe -m pytest tests/test_generator.py -x -q --no-cov`
  </acceptance_criteria>
  <done>generate_all() runs topic synthesis by default. INDEX.md links to topic pages with frontmatter title. synthesize=False preserves old behavior. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --no-synthesize flag to CLI generate command and update run command</name>
  <files>src/github_pr_kb/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/github_pr_kb/cli.py (full file - generate command, _run_generate helper, run command)
    - tests/test_cli.py (full file - existing CLI test patterns)
    - src/github_pr_kb/generator.py (generate_all signature after Task 1)
  </read_first>
  <behavior>
    - Test: `generate` command without flags calls generate_all(synthesize=True) by default
    - Test: `generate --no-synthesize` calls generate_all(synthesize=False)
    - Test: `generate --no-synthesize --regenerate` passes both flags correctly
    - Test: `run` command calls generate with synthesize=True
    - Test: CLI output includes topics_written and topics_skipped in summary when synthesis runs
    - Test: --help for generate shows --no-synthesize option with description
  </behavior>
  <action>
    1. Update `_run_generate()` helper to accept `synthesize` parameter (per D-05):
       ```python
       def _run_generate(regenerate: bool = False, synthesize: bool = True) -> str:
       ```
       Pass `synthesize` to `generator.generate_all(regenerate=regenerate, synthesize=synthesize)`.

    2. Update summary string in `_run_generate()` to include topic counts when synthesis ran:
       ```python
       parts = [f"Generated {result.written} new, {result.skipped} skipped, {result.filtered} filtered, {len(result.failed)} failed."]
       if synthesize:
           parts.append(f"Topics: {result.topics_written} written, {result.topics_skipped} unchanged.")
       return " ".join(parts)
       ```

    3. Add `--no-synthesize` flag to `generate` command (per D-05):
       ```python
       @click.option(
           "--no-synthesize",
           is_flag=True,
           help="Skip topic synthesis; produce per-comment articles only (for debugging or cost-sensitive runs).",
       )
       def generate(regenerate: bool, no_synthesize: bool, verbose: bool) -> None:
       ```
       Pass `synthesize=not no_synthesize` to `_run_generate()`.

    4. Update `run` command's generate step to pass `synthesize=True` explicitly (already the default, but be explicit for clarity).

    5. Write CLI tests:
       - Test default generate calls with synthesize=True
       - Test `--no-synthesize` calls with synthesize=False
       - Test combined `--no-synthesize --regenerate`
       - Mock KBGenerator to avoid needing real classified files / API key

    6. Run `uv run ruff check src/ tests/ --fix --exit-non-zero-on-fix` to fix any lint issues.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_cli.py -x -q --no-cov -k "no_synthesize or generate"</automated>
  </verify>
  <acceptance_criteria>
    - grep "no.synthesize" src/github_pr_kb/cli.py returns matches for the flag definition
    - grep "synthesize" src/github_pr_kb/cli.py shows synthesize parameter passed to _run_generate and generate_all
    - grep "topics_written\|topics_skipped" src/github_pr_kb/cli.py shows topic counts in output summary
    - grep "test.*no_synthesize" tests/test_cli.py returns test function matches
    - `github-pr-kb generate --help` output includes --no-synthesize option
    - All tests pass: `.venv/Scripts/python.exe -m pytest tests/ -x -q --no-cov`
    - `uv run ruff check src/ tests/` passes clean
  </acceptance_criteria>
  <done>--no-synthesize flag on generate command skips topic synthesis. Default behavior synthesizes. CLI output includes topic counts. All tests pass, ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> generator | User-provided flags control synthesis behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | --no-synthesize flag | accept | Boolean flag with no security implications; controls output format only |
| T-09-08 | Information Disclosure | INDEX.md | accept | Index contains titles and relative paths only; no secrets or PII; same exposure as current per-comment index |
</threat_model>

<verification>
1. `.venv/Scripts/python.exe -m pytest tests/ -x -q --no-cov` - full suite passes
2. `uv run ruff check src/ tests/ --fix --exit-non-zero-on-fix` - no lint errors
3. End-to-end: generate_all(synthesize=True) produces topic pages + INDEX.md with topic titles
4. End-to-end: generate_all(synthesize=False) produces per-comment articles (backward compat)
5. CLI: `github-pr-kb generate --help` shows --no-synthesize
</verification>

<success_criteria>
- generate_all() synthesizes topics by default
- --no-synthesize flag produces per-comment articles only
- INDEX.md links to topic pages with title from frontmatter
- run command uses synthesis by default
- CLI output shows topic synthesis counts
- Full test suite passes with no regressions
- ruff check clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-wiki-style-kb-synthesis-merge-related-articles-into-compound/09-03-SUMMARY.md`
</output>
